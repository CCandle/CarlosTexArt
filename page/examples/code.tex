\subsection{代码清单}
\label{sec:code}

内联代码可以写成 \lstinline|adc_value = read_adc();|。
如 \autoref{lst:pid-controller} 所示，可以直接引用外部源码文件。

\lstinputlisting[
    style=Python,
    caption={PID 控制器实现},
    label={lst:pid-controller}
]{src/pid_controller.py}
